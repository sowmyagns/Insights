/**
 * @deprecated Prefer `Button` from `./Button`.
 * Kept as a thin alias so existing imports keep working.
 */
export {
  default,
  PrimaryButton,
  SecondaryButton,
  SuccessButton,
  WarningButton,
  DangerButton,
  IconButton,
} from "./Button";
